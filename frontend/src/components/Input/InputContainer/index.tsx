import { flex } from '@/utils/flex'
import styled from 'styled-components'

const InputContainer = styled.section`
	${flex('column', '', 'flex-start', '8px')}
	width: 100%;
`

export default InputContainer
